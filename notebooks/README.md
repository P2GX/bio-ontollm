# README

To run these notebooks, we recommend using VS Code. The most straightforward way of installing the required packages is to enter the following commands

```{bash}
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements
````

Then set the notebooks to use this kernel by clicking on Set kernel (upper right) -> Python environments -> venv.

